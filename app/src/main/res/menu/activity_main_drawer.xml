<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:id="@+id/drawer_group_first"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_find_partner"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/find_partner" />
    </group>

    <group android:id="@+id/drawer_group_second"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_active_friends"
            android:icon="@drawable/ic_menu_camera"
            android:title="@string/friends" />
        <item
            android:id="@+id/nav_requested_friends"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/friend_requests" />
    </group>

    <group android:id="@+id/drawer_group_third"
        android:checkableBehavior="single">
        <item
            android:id="@+id/nav_active_matches"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/matches" />
        <item
            android:id="@+id/nav_requested_matches"
            android:icon="@drawable/ic_menu_gallery"
            android:title="@string/match_requests" />
    </group>

    <!--<item android:title="Communicate">
        <menu>
            <item
                android:id="@+id/nav_share"
                android:icon="@drawable/ic_menu_share"
                android:title="Share" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="Send" />
        </menu>
    </item>-->

</menu>
